{
  "title": "project log entry envelope",
  "type": "object",
  "properties": {
    "message": {
      "type": "object",
      "properties": {
        "project": {
          "title": "project discovery key",
          "type": "string",
          "pattern": "^[a-f0-9]{64}$"
        },
        "index": {
          "title": "log entry index",
          "type": "integer",
          "minimum": 0
        },
        "body": {
          "title": "log entry payload",
          "type": "object"
        }
      },
      "required": [
        "project",
        "index",
        "body"
      ],
      "additionalProperties": false
    },
    "publicKey": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "signature": {
      "type": "string",
      "pattern": "^[a-f0-9]{128}$"
    }
  },
  "required": [
    "message",
    "publicKey",
    "signature"
  ],
  "additionalProperties": false
}
